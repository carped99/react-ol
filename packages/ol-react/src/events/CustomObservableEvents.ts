/**
 * 사용자 정의 이벤트 타입을 위한 인터페이스
 *
 * @category Events
 */

// eslint-disable-next-line @typescript-eslint/no-empty-object-type
export interface CustomObservableEvents extends Record<string, never> {}
